# this seems wrong, build this up
git config --global diff.tool bc3

# in a personal repo, make sure to set the user so default global settings are not used instead
git config --local user.email=hard2737@gmail.com
git config --local user.name=hard2737@gmail.com




